allprojects {
    apply plugin: 'maven'
    
    group = 'com.github.davidnortonjr.gradleproblem'
    version = '1.0.0-SNAPSHOT'
}

subprojects {
	apply plugin:'groovy'

	repositories{
	    mavenCentral() 
	}
}

task wrapper(type: Wrapper) {
    gradleVersion = '1.11'
}
